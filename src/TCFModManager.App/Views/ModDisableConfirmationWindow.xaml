<ui:FluentWindow
    x:Class="TCFModManager.App.Views.ModDisableConfirmationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="520"
    MaxHeight="640"
    SizeToContent="Height"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    ExtendsContentIntoTitleBar="True">
    <!--
        Shown before a disable/enable that affects other mods, following the same plain modal
        FluentWindow pattern as ReadModPageConfirmationWindow - ShowDialog() from any call site,
        result read back through a static Confirm helper.
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            x:Name="WindowTitleBar"
            Grid.Row="0"
            Title="Check dependencies" />

        <StackPanel
            Grid.Row="1"
            Margin="24,12,24,24">
            <TextBlock
                x:Name="SummaryText"
                TextWrapping="Wrap"
                Margin="0,0,0,4" />

            <TextBlock
                x:Name="TargetsText"
                TextWrapping="Wrap"
                Opacity="0.7"
                Margin="0,0,0,16" />

            <TextBlock
                x:Name="ImpactHeading"
                TextWrapping="Wrap"
                FontWeight="SemiBold"
                Margin="0,0,0,8" />

            <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ImpactList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ui:SymbolIcon
                                    Grid.Column="0"
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    FontSize="16"
                                    Symbol="{Binding Glyph}"
                                    Foreground="{Binding Brush}" />

                                <StackPanel Grid.Column="1">
                                    <TextBlock
                                        FontWeight="SemiBold"
                                        TextTrimming="CharacterEllipsis"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        FontSize="11"
                                        Opacity="0.7"
                                        TextWrapping="Wrap"
                                        Text="{Binding Detail}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock
                TextWrapping="Wrap"
                Opacity="0.7"
                Margin="0,12,0,0"
                Text="Nothing is deleted either way - disabled mods keep their own files and configs, and move back untouched when re-enabled." />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <ui:Button
                    Margin="0,0,8,0"
                    Content="Cancel"
                    Click="CancelButton_Click" />

                <ui:Button
                    x:Name="ProceedOnlyButton"
                    Margin="0,0,8,0"
                    Click="ProceedOnlyButton_Click" />

                <ui:Button
                    x:Name="CascadeButton"
                    Appearance="Primary"
                    Click="CascadeButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
